// Calculating area of a circle

let radius = 4

const area = Math.round(Math.PI * (radius*radius))

console.log(`Area of the circle having radius ${radius} is ${area}`);

// Output : Area of the circle having radius 4 is 50